# Batch A Foundations — render report

Generated by `scripts/intel_video_pipeline.py` with Edge TTS `en-AU-WilliamNeural`,
1080p 16:9 h264 + AAC. MP4s are **not** in git (see `.gitignore`: `videos/out/`).

## Outputs

Canonical files on this agent (gitignored):

| Lesson | Path | Duration | Size |
|---|---|---|---|
| what-is-intelligence | `videos/out/what-is-intelligence-lesson-16x9.mp4` | 4:51 (291.3s) | 21.2 MB (22,226,323 bytes) |
| intelligence-cycle | `videos/out/intelligence-cycle-lesson-16x9.mp4` | 12:23 (743.3s) | 42.1 MB (44,184,515 bytes) |
| intelligence-types | `videos/out/intelligence-types-lesson-16x9.mp4` | 9:31 (570.7s) | 42.7 MB (44,723,042 bytes) |
| intelligence-vs-information | `videos/out/intelligence-vs-information-lesson-16x9.mp4` | 7:27 (447.3s) | 30.9 MB (32,429,887 bytes) |
| intelligence-decision-making | `videos/out/intelligence-decision-making-lesson-16x9.mp4` | 6:58 (418.2s) | 33.5 MB (35,167,902 bytes) |

Artifact copies (when the store accepted them): `/opt/cursor/artifacts/batch-a-foundations/`.
The workspace `videos/out/` copies are the source of truth if artifact I/O failed on the larger files.

All five probe as `h264` 1920×1080 + `aac` 44.1 kHz stereo.

Re-render:

```bash
python3 -m pip install -r scripts/requirements-video.txt
python3 scripts/intel_video_pipeline.py \
  --slug what-is-intelligence \
  --slug intelligence-cycle \
  --slug intelligence-types \
  --slug intelligence-vs-information \
  --slug intelligence-decision-making \
  --out videos/out
```

## Image-relevance gaps

- **intelligence-cycle:** later phases reuse weaker stills after the three `intelligence-cycle*` images are spent. Phase 3 Processing → `data-organization.png` (score 2). Phase 5 Dissemination (first pass) → `financial-intelligence-banner.png` because the business-exercise body mentions financial filings.
- **intelligence-types:** SIGINT / GEOINT / TECHINT *Collection Methods* subsections have no unused on-discipline PNG (`sigint-basics.png` is an identical title-card fingerprint of `enhanced-3d.png`; other GEOINT/TECHINT files are thumbs or stubs). Those three segments use a generated navy fallback.
- **intelligence-vs-information:** Information Overload → `cognitive-bias-concept.png` (score 2).
- **intelligence-decision-making:** Cybersecurity / Politicization / Decision Feedback score 2 against generic analysis/office stills. No dedicated cybersecurity or politicization concept PNG in `public/`.

Discipline acronyms (HUMINT/OSINT/SIGINT/GEOINT/MASINT/FININT/TECHINT) otherwise match the right illustrations.

## TTS quirks

- William Neural is clean; no “asterisk” / “vertical bar” reads after markdown strip.
- `intelligence-cycle` page embeds a JS `${['Direction',…].map(…)}` list inside the template literal. The extractor now walks nested backticks/braces and expands it to “Phase 1: Direction …” so TTS never says “dollar brace”.
- Long sections were split for Edge TTS: what-is-intelligence (2 chunks), cycle Dissemination (2), vs-information Interpretation (3).
- `intelligence-types` skipped the trailing `Next Topic: HUMINT…` nav line.
- No commercial sentences (prices, Stripe, Buy, waitlist, student counts, agency clients, pass guarantees) were present in these five lessons.

## Blocked / skipped images

Image library load: **206 usable**, **302 skipped**. Typical reasons:

- `*-thumb*`, `banner-large`, learning-path thumbnails, mascots
- Tiny branded navy+orange stubs (~12 KB): `non-existent-image.png`, `humint-friends.png`, `osint-stalking.png`, …
- Identical fingerprints: `sigint-basics.png` == `enhanced-3d.png`; several OSINT/GEOINT copies of the same art
- Unreadable / meme files (`competing-hypotheses-drake.png` is not a valid PNG)

## Notes

Lesson markdown was not rewritten. Dry humour already in the topic pages is spoken as written; nothing slapstick was invented.
